# MySQL Database Connection Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/intern_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=ibrahimfazil

# JPA (Java Persistence API) Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Additional Hibernate Properties (optional)
spring.jpa.properties.hibernate.format_sql=true
server.address=0.0.0.0

# Custom path for the OpenAPI JSON documentation
#springdoc.api-docs.path=/api-docs

# Custom path for Swagger UI
#springdoc.swagger-ui.url=/swagger-ui.html

